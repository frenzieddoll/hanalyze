-- Placeholder — not referenced by any test-suite stanza.
-- The doctest runner is deferred until cabal/doctest package-db
-- integration can be wired up cleanly (see CHANGELOG H5 note).
module Main where
main :: IO ()
main = return ()
