# Benchmark comparison environment for hanalyze.
# Install via:
#   python3 -m venv bench/venv
#   bench/venv/bin/pip install -r bench/requirements.txt

# --- foundation ---
numpy
scipy
pandas
matplotlib
pyperf
tabulate

# --- regression / ML ---
scikit-learn
statsmodels
pygam

# --- kernel / GP ---
# GPy: dropped (pins scipy<1.13 with no Python 3.14 wheels — would force a
# from-source scipy build that fails on Arch py3.14). sklearn's
# GaussianProcessRegressor remains the primary GP comparison.

# --- single-objective optimization ---
cma
pyswarms

# --- Bayesian optimization ---
scikit-optimize

# --- multi-objective optimization ---
pymoo

# --- variational inference (PyMC-free, JAX backend) ---
jax
jaxlib
numpyro

# --- MCMC comparison (HMC/NUTS, planned B7) ---
# pymc: most popular Python PPL with NUTS. May not have wheels for the
#       newest Python versions; in that case fall back to blackjax / numpyro.
# blackjax: lightweight JAX-based HMC/NUTS, robust install.
# arviz: WAIC / PSIS-LOO / ESS / R-hat. Also doubles as a sanity-check
#        for our own Stat.MCMC and Stat.ModelSelect outputs.
pymc
blackjax
arviz

# --- survival analysis comparison (planned B8) ---
# lifelines: Kaplan-Meier, Nelson-Aalen, Cox PH, log-rank.
lifelines
