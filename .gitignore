/dist-newstyle/
*.csv
*.html
*.txt
*.png
*.svg

# Track demo data CSVs under data/ (reproducible inputs)
!data/**/*.csv

# Ignore the trash directory entirely (output artifacts; user-managed cleanup)
/trash/

# データ生成器は git 管理外 (確認用、CSV だけ commit する)
/demo/io/PotentialGen.hs
